# Future R package

## Summary function for stats
## evaluate function for plots etc.

### Plan functions ==>
# - The functions below create a 'plan' to be executed (much like dbplyr does for sql database). A plan & all prep is done before the user calls 'execute' on a blm object.
# - All error checks are done in R, all computation is done in Julia. (could make this an optional for speedup purposes).
# 





# ---------- S3 generics -----------------------


# ---------- Core BLM functions -----------------




# Find a way to compute user-specific variables.


# See:
#  chrome-extension://oemmndcbldboiebfnladdacbdfmadadm/https://arxiv.org/pdf/1311.4780.pdf